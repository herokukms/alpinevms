name: Build for W64

on: [push]
  # workflow_dispatch:
  #   inputs:
  #     logLevel:
  #       description: 'Log level'
  #       required: true
  #       default: 'warning'
  #       type: choice
  #       options:
  #       - info
  #       - warning
  #       - debug

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-w64:
    runs-on: ubuntu-latest
    permissions: write-all

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Get current date
        id: date
        run: echo "::set-output name=date::$(date +'%Y-%m-%d')"
      - name: Build w64 executables
        run: ./build-forlinux64
      - name: Zip binaries
        run: zip --junk-paths vlmcs bin/vlmcs bin/vlmcsd
      - name: Create Release
        uses: "marvinpinto/action-automatic-releases@latest"
        with:
          repo_token: "${{ secrets.GITHUB_TOKEN }}"
          automatic_release_tag: nightly-tag-${{ steps.date.outputs.date }}
          prerelease: false
          title: "Linux 64"
          files: |
            vlmcs.zip